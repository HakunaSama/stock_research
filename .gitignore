# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv

# Secrets / credentials — NEVER commit real API keys
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
credentials.json

# SQLite (user DB, sessions, jobs) — runtime state, never commit
*.db
*.db-wal
*.db-shm

# Node / frontend
node_modules/
dist/
dist-ssr/
*.local

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
.cursor/

# Local package-manager cache
.pnpm-store/

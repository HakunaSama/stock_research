# Backend runtime dependencies for the stock-research webapp.
# The ODR engine itself is stdlib-only; these add the web/serving layer.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
requests>=2.31        # used by vendored stocksdk (K-line data sources)
Pillow>=10.3,<13      # validated/resized avatar image processing
